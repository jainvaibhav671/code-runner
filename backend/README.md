# Code Runner Backend

## Setup Instructions

- Install dependencies
  ```bash
  npm install
  ```
- Start the server
  ```bash
  http://localhost:3000
  ```

> The server starts at [http://localhost:3000](http://localhost:3000)
